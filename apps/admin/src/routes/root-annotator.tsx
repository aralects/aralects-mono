import { AppContainer, AppContent, AppHeader } from "@/components/AppContainer";

export const RootAnnotator = () => {
  return (
    <AppContainer>
      <AppHeader />
      <AppContent className="grid grid-cols-[300px_1fr]">{/*  */}</AppContent>
    </AppContainer>
  );
};
